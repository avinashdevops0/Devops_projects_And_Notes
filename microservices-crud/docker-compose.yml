version: "3.9"

services:
  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.d
    ports:
      - "80:80"
    depends_on:
      - frontend
      - gateway

  frontend:
    build: ./frontend

  gateway:
    build: ./gateway
    depends_on:
      - users-service
      - products-service

  users-service:
    build: ./users-service

  products-service:
    build: ./products-service
